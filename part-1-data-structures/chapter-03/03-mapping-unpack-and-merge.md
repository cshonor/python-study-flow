# 映射拆包与合并：PEP 448（`**`）与 PEP 584（`|` / `|=`）

> **本篇定位**：《流畅的 Python》**3.2.2–3.2.3**：Python 3.5+ 在**函数调用**与**字典字面量**中的 `**` 拆包；Python 3.9+ 的字典**并集运算符** `|` 与原地 `|=`。  
> **前置**：字典推导式见 `02-dict-comprehension.md`。  
> **配套脚本**：`dict_unpack_merge_demo.py`（非 3.9+ 时对 `|` 相关段落会跳过）。

---

## 一、与字典推导式衔接（3.2.1 回顾）

- 可把「键值对」可迭代对象直接交给 **`dict(...)`** 构造。  
- 复杂变换用**字典推导式**更顺手（对调、过滤、`.upper()` 等），详见 `02-dict-comprehension.md`。

**注意**：若 `country_dial` 已是「国名 → 区号」的 `dict`，要按**国名排序**再推导，应对 **`items()`** 排序，而不是 `for code, country in sorted(country_dial)`（`sorted(dict)` 只给出**键**，不能拆成两个变量）：

```python
{country.upper(): code for country, code in sorted(country_dial.items()) if code < 70}
```

（与 `02` 中「以区号为键、国名为大写」的写法只是**键值角色不同**，按需求选用。）

---

## 二、3.2.2 映射拆包（PEP 448）

Python 3.5 起，`**` 可用于：

### 1. 函数调用：多个 `**` 与关键字实参混用

- 可将**多个**映射拆成关键字参数传入；**靠后的覆盖靠前**的同名键。  
- 键须是合法的关键字参数名（通常为字符串键；细节见语言参考）。

```python
def dump(**kwargs):
    return kwargs

dump(**{"x": 1}, y=2, **{"z": 3})  # {'x': 1, 'y': 2, 'z': 3}
```

### 2. 字典字面量：`{**a, **b}`

- 在 `{}` 里可写多个 `**mapping`，**后面的键覆盖前面的**。  
- 可与普通 `键: 值` 项混写，顺序决定优先级。

```python
{"a": 0, **{"x": 1}, "y": 2, **{"z": 3, "x": 4}}
# {'a': 0, 'x': 4, 'y': 2, 'z': 3}   # 右侧的 x=4 覆盖左侧的 x=1
```

---

## 三、3.2.3 使用并集运算符合并（PEP 584，Python 3.9+）

### 1. 二元 `|`：新字典

- 返回**新** `dict`，**不修改**左右操作数（除非左操作数是子类且重载了行为，一般按内置语义理解即可）。  
- 键冲突时，**以右操作数为准**。

```python
d1 = {"a": 1, "b": 3}
d2 = {"a": 2, "b": 4, "c": 6}
d1 | d2   # {'a': 2, 'b': 4, 'c': 6}
```

### 2. `|=`：原地更新

- **修改**左侧字典，并入右侧映射；同样**右覆盖左**。  
- 右侧可为任意**映射**对象（见 PEP 584）。

```python
d1 = {"a": 1, "b": 3}
d2 = {"a": 2, "b": 4, "c": 6}
d1 |= d2
# d1 现为 {'a': 2, 'b': 4, 'c': 6}
```

### 3. 版本与兼容

- **`|` / `|=`** 需 **Python 3.9+**。  
- 3.8 及更早：用 **`{**d1, **d2}`** 做一级浅合并，或 `d1 = {**d1, **d2}` / `copy` + `update`。

---

## 四、对照表

| 方式 | 最低版本 | 是否新对象 | 重复键 |
| :--- | :--- | :--- | :--- |
| `{**d1, **d2}` | 3.5+ | 是 | 后覆盖前 |
| 字典并集（`|`） | 3.9+ | 是 | 右覆盖左 |
| 原地并集（`|=`） | 3.9+ | 否（改左侧） | 右覆盖左 |
| `d1.update(d2)` | 2+ | 否 | 右覆盖左 |

---

## 五、实用拓展

### 1. 兼容 3.5–3.8

```python
merged = {**d1, **d2}
```

### 2. 嵌套字典

上述写法都是**浅合并**：同名键上的**内层 dict**会被**整段替换**，不会递归合并子键。

### 3. 其它模式

- **`collections.ChainMap`**：多映射**按顺序查找**的视图，不是物化合并。  
- **`dict.update`**：原地更新，返回 `None`。

---

## 六、可运行对照

见 `dict_unpack_merge_demo.py`（PEP 448、`{**d1, **d2}`、`|` / `|=`、`update`、`ChainMap`）。
