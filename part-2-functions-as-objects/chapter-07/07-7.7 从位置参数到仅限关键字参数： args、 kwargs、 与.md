# 7.7 从位置参数到仅限关键字参数：`*args`、`**kwargs`、`*` 与 `/`

这一节讲的不是“语法花活”，而是 **Python API 设计能力**：你如何用参数规则让调用更安全、更清晰、更不容易被误用。

本节围绕书里的 `tag` 函数（示例 7-9、7-10）讲清楚：

- `*content`：可变位置参数
- `class_`：仅限关键字参数 + 关键字冲突的工程解法
- `**attrs`：可变关键字参数
- `/`：仅限位置参数（Python 3.8+）

配套脚本：`07_tag_and_positional_only_demo.py`（**`demo_beginner_walkthrough`**；**`demo_args_vs_kwargs_plain`**（**大白话 §9·三**）；**`demo_parameter_kinds_minimal`** / **`demo_full_parameter_template`**）。

---

## 大白话新手版（不讲术语顺序；与 **§零** 说的是同一套规则）

### 1、先认 4 个符号，记死

1. **`/`**：**斜杠左边**的参数，调用时**只能按顺序塞位置值**，不能写 **`名字=值`**。  
2. **裸 `*`**（后面不跟 `args` 这个名字也行）：**星号右边**的参数，调用时**必须写 `名字=值`**。  
3. **`*args`**：多出来的**位置**实参，全收进来，变成一个**元组**（名字不一定叫 `args`，习惯如此）。  
4. **`**kwargs`**：多出来的 **`名字=值`**，全收进来，变成一个**字典**；在定义里**永远排在最后**。

### 2、定义里参数排队顺序（新手先背骨架）

从左到右常见合法骨架是：

1. **只能按位置传的**（写在 **`/`** 的左侧）  
2. **普通参数**（位置、关键字两种写法一般都可以，除非被上面的规则夹住）  
3. **可选：`*args`** 收「多出来的位置」  
4. **`/` 与 `*args` 后面的形参**：对调用者来说往往变成**必须写 `名字=值`** 的区间（**仅限关键字**）  
5. 形参表**最后**写 **可变关键字参数**（习惯写成 ``**kwargs``）：收「多出来的关键字」  

不想用 **`*args`**、又想把后面一刀切成「只能关键字」时，就写个**裸 `*`** 挡在中间（见 **§零·二** 小例）。

### 3、最经典：`tag(name, *content, class_=None, **attrs)` 用人话拆

- **`name`**：第一个名字，比如 **`"p"`**、**`"img"`**。  
- **`*content`**：从第二个位置实参开始，**一串正文**都可以往里丢，函数里收到的是**元组**。  
- **`class_`**：想传 HTML 的 **`class`** 属性，调用时必须写 **`class_=...`**；参数名带下划线是因为 **`class` 是 Python 保留字**，不能直接当形参名。  
- **`attrs`**：其它所有 **`xxx=...`** 关键字，统统进**字典**。

### 4、什么叫「仅限关键字」

只要形参出现在 **裸 `*`** 或 **`*args`** 的**右边**，调用时一般就**不能只丢一个光秃秃的值**，而要写 **`形参名=值`**（具体以解释器报错提示为准）。

### 5、什么叫「仅限位置」（`/`）

```python
def f(a, b, /):
    pass


f(1, 2)        # 可以：按位置
# f(a=1, b=2)  # 不行：/ 左侧禁止关键字实参
```

用处（记方向）：**不想让用户依赖参数名**；以后你改形参名字，**按位置调用的人**不容易被你改挂。

### 6、新手必踩坑：生成器别拿来判断「有没有内容」

```python
attr_pairs = (f"{k}={v}" for k, v in attrs.items())
if attr_pairs:  # 错：生成器对象几乎恒为「真」，判不出里面有没有元素
    ...
```

应判断 **`if attrs:`**，或先 **`list(...)`** 再判断。正文 **§二**、**§零·六** 同一件事。

### 7、六条最简总结

1. **`/` 左边**：只能**按位置**传，不要写 **`名=值`**。  
2. **`*` 右边**：一般要 **`名=值`**。  
3. **`*args`**：多出来的位置进**元组**；**`kwargs`**：多出来的关键字进**字典**，且**在最后**。  
4. **`*args`（或裸 `*`）后面的形参**：调用侧通常就当**仅限关键字**记。  
5. **`class_`**：避开保留字 **`class`** 的常规写法。  
6. **生成器对象**：别当「空不空」的判断依据。

### 8、可复制的新手示例（逐行注释）

见脚本里的函数 **`demo_beginner_walkthrough`**（与上面的 **`tag`**、**`/`** 小例一致，运行即打印说明）。

### 9、`*args` 和 `**kwargs`：人话一秒分清（不绕弯）

#### 9·一、先分清「长相」

- **`*args`**：形参名前**一个**星号（`args` 只是习惯名，可以换）。  
- **`**kwargs`**：形参名前**两个**星号（`kwargs` 同理可换）。

#### 9·二、各自收什么（核心区别）

**`*args`：收「多出来的位置实参」→ 元组**

调用时**不带「`名字=值`」**、**按顺序**塞进去的那一串，在**前面的普通形参、`/` 规则等都喂饱之后**，**剩下的位置实参**会进 **`*args`**，在函数体里通常是 **`tuple`**。

**特例**：若签名写成 **`def f(*args, **kwargs):`**，且调用 **`f(10, 20, 30)`**，那这三个都会进 **`args`**，得到 **`(10, 20, 30)`**。若前面还有 **`name`** 这类普通形参，则会**先填 `name`**，**多出来的**才进 **`*args`**（见 **`tag(name, *content, ...)`**）。

**`**kwargs`：收「多出来的关键字实参」→ 字典**

调用时写成 **`名字=值`** 的那些，在**已经对应到具名关键字形参之后**，**剩下的「无名份」关键字实参**会进 **`**kwargs`**，在函数体里通常是 **`dict`**。

#### 9·三、最简例子（一看就懂）

```python
def f(*args, **kwargs):
    print("args:", args)
    print("kwargs:", kwargs)


f(1, 2, 3, name="李四", city="北京")
# args: (1, 2, 3)
# kwargs: {'name': '李四', 'city': '北京'}
```

运行见脚本 **`demo_args_vs_kwargs_plain`**。

#### 9·四、两句口诀（背）

- **一个星号 `*args`**：多出来的**位置**，打成**元组**。  
- **两个星号 `**kwargs`**：多出来的 **`键=值`**，打成**字典**。

#### 9·五、顺序不能乱

形参表里常见骨架是：**普通参数 → `*args`（可选）→ 仅限关键字段（可选）→ `**kwargs`（最后）**。  
**不能**把 **`**kwargs`** 写在 **`*args` 前面**（语法直接非法）。

---

## 零、终极笔记（背诵 + 总览；与 **§一～§五** 对照）

### 零·一、本节核心一句话

**Python 用参数在签名里的位置 + 两个符号 `/` 与 `*`，约束「只能怎么传」。**  
再配合 **`*args` / `**kwargs`** 收「多出来的位置 / 关键字」。

| 符号 / 形态 | 含义（调用侧） |
|-------------|----------------|
| **`/`** | **其左侧**：**仅限位置**——不能写 `name=...` 这种关键字实参 |
| **裸 `*`**（后面没有名字） | **其右侧**：**仅限关键字**——必须写 `kw=...` |
| **`*args`** | 收「多出来的位置实参」→ **`tuple`** |
| **`**kwargs`** | 收「多出来的关键字实参」→ **`dict`** |

### 零·二、合法顺序（背骨架；`*args` 整段可省略）

[官方语法](https://docs.python.org/3/reference/compound_stmts.html#function) 大意如下（**`**kwargs` 必须最后**）：

```python
def func(
    a, b, /,              # 1 仅限位置
    c, d=0,              # 2 位置或关键字（可有默认值）
    *args,               # 3 可变位置（可整块省略）
    e, f=1,              # 4 仅限关键字（出现在 *args 之后）
    **kwargs,            # 5 可变关键字（最后）
) -> None:
    ...
```

### 零·二·附、把上面这段模板**逐段**讲透（Python 3.8+）

**先纠一句容易抄错的地方**：这个「全家桶」签名里 **`c` 与 `e` 都是必填**（没有默认值），所以**不存在**合法的 **`func(1, 2)`**——只把 **`a,b`** 填满还不够，后面还得把 **`c`** 和 **`e=`** 安排上。下面分段的**最小合法调用**会写清楚。

---

#### 1）`a, b, /` —— 仅限位置（positional-only）

**含义**：**`/` 左边**的形参，调用时**只能按位置传**，不能写 **`a=...`**、**`b=...`**。

**单独看它**（不包含后面的 **`c`/`e`** 时），可以这样记：

```python
def pos_only(a, b, /) -> int:
    return a + b


pos_only(1, 2)        # 合法
# pos_only(a=1, b=2)  # TypeError：/ 左侧禁止关键字实参
```

套回「全家桶 **`func`**」时：**仍然必须**再给出 **`c`** 以及 **`e=`**（见下文 **4）** 与脚本 **`demo_full_parameter_template`**）。

---

#### 2）`c, d=0` —— 位置或关键字（可有默认值）

**含义**：在 **`/` 右侧**、**`*args` 左侧**这一段里，**位置 / 关键字**一般都能用；**有默认值的写在更靠后**（如 **`d=0`**）。

**合法示意**（注意：**每次都要带 `e=`**，否则 **`e` 没着落**）：

```python
func(1, 2, 3, e=4)           # c=3，d 用默认 0
func(1, 2, c=3, e=4)        # c 改关键字也行
func(1, 2, d=5, c=3, e=4)    # c、d 都用关键字，顺序可换
```

---

#### 3）`*args` —— 可变位置

**含义**：把「**填完前面固定形参之后，多出来的位置实参**」收成**元组**；可以不传，此时 **`args == ()`**。

**注意**：一旦签名里写了 **`*args`**，**后面就不能再出现「还能吃位置实参的普通形参」**；**`e`、`f`** 只能走**关键字**（见 **4）**）。

---

#### 4）`e, f=1` —— 仅限关键字（keyword-only）

**含义**：出现在 **`*args`**（或**裸 `*`**）**之后**的形参，调用时**必须用 `名字=值`**，**不能指望再「按顺序多塞一个位置」把它塞给 `e`**。

```python
# func(1, 2, 3, 4, 5)  # 错：这里的 5 会进 *args，e 仍然没关键字实参 → TypeError
func(1, 2, 3, e=4)        # 合法：e=4，f 用默认 1
func(1, 2, 3, e=4, f=5)   # 合法
```

---

#### 5）`**kwargs` —— 可变关键字（必须最后）

**含义**：把「**签名里没写出来的那些 `名字=值`**」收成**字典**；在形参表里**排在最后**（前面可有尾随逗号，见官方语法）。

---

#### 合法「全家桶」调用一例（把五段都用上）

```python
def func(a, b, /, c, d=0, *args, e, f=1, **kwargs) -> None:
    print(a, b, c, d, args, e, f, kwargs)


func(
    1,
    2,  # a, b（仅限位置）
    3,
    4,  # c=3, d=4
    5,
    6,  # 多出来的位置进 *args → (5, 6)
    e=7,
    f=8,  # e、f（仅限关键字）
    x=9,
    y=10,  # 其余关键字进 **kwargs
)
# 输出：1 2 3 4 (5, 6) 7 8 {'x': 9, 'y': 10}
```

**一句话记顺序（不能乱）**：**仅限位置 → 普通/默认 → `*args` → 仅限关键字 → `**kwargs`**。

**五种各一例的最小可运行代码**：见 **`07_tag_and_positional_only_demo.py`** 里的 **`demo_parameter_kinds_minimal`** 与 **`demo_full_parameter_template`**。

---

**没有 `*args`、只想关键字区**时，用**裸 `*`** 隔开：

```python
def only_kw(a, b, *, c, d) -> None:
    ...

# only_kw(1, 2, 3, 4)     # TypeError：c、d 必须关键字
only_kw(1, 2, c=3, d=4)  # 合法
```

这就是书里 **`tag`** 一类 API 的底层规则。

### 零·三、`tag(name, *content, class_=None, **attrs)` 逐段（示例 7-9）

```python
def tag(name, *content, class_=None, **attrs):
    ...
```

| 段 | 含义 | 调用时注意 |
|----|------|------------|
| **`name`** | 普通形参 | 位置、关键字均可（除非你给前面加 `/`） |
| **`*content`** | 可变位置 | `tag("p", "a", "b")` 里 `"a"`,`"b"` 进 tuple；**它后面的形参默认变成仅限关键字** |
| **`class_`** | 给 HTML `class` 留的槽 | **必须** `class_=...`；下划线是因为 **`class` 是保留字** |
| **`**attrs`** | 其余关键字 → **`dict`** | `tag("img", src="x.png", title="...")` |

**实现细节**：仓库里的可运行版本在 **`07_tag_and_positional_only_demo.py`**（对 `attrs` 做了 `sorted`、换行拼接等，与书上打印格式可能略有差别，**参数形态一致**）。

### 零·四、`/` 与「裸 `*`」最小例

```python
def my_divmod(a, b, /):
    return (a // b, a % b)

my_divmod(10, 3)        # 合法
# my_divmod(a=10, b=3)  # TypeError：/ 左侧禁止关键字实参
```

### 零·五、参数类型速查表（背诵版）

| 语法 | 名字 | 调用特点 |
|------|------|------------|
| `def f(a, /):` | 仅限位置 | 只能 `f(1)`，不能 `f(a=1)` |
| `def f(a):` | 普通 | 位置、关键字均可 |
| `def f(*args):` | 可变位置 | 多出来的位置进 `tuple` |
| `def f(*, a):` 或 `def f(*args, a):` | 仅限关键字 | 必须 `f(a=1)`（在 `*` 或 `*args` 之后） |
| `def f(**kwargs):` | 可变关键字 | 多出来的关键字进 `dict`（总在最后） |

### 零·六、生成器别当「有没有内容」的判断（与 **§二** 同一坑）

```python
attr_pairs = (f'{k}="{v}"' for k, v in attrs.items())
if attr_pairs:  # 错：生成器对象恒为真，即使内部没有元素
    ...
```

应判断 **`attrs`** 是否为空，或先 **`list(...)`** 再判断。详见 **§二**。

### 零·七、三条结论（面试 / 写 API）

1. **`*content` 之后的形参**（如 `class_`）对调用者而言是**仅限关键字**（除非再引入更怪的 `/` 组合，书里 `tag` 不需要）。  
2. **`class_`** 是避开关键字 **`class`** 的常规写法，体内再写进 **`attrs["class"]`**。  
3. **签名里从前往后**：**仅限位置** → **位置或关键字** → **`*args`（可选）** → **仅限关键字** → **`**kwargs`（最后）**。

---

## 一、`tag`：一个“参数特性大全”的实战例子（示例 7-9）

目标：生成一个或多个 HTML 标签字符串。

函数签名（重点在参数形态）：

```python
def tag(name, *content, class_=None, **attrs):
    ...
```

你可以把它拆成四块理解：

| 形态 | 名字 | 含义 | 调用时怎么传 |
|---|---|---|---|
| 普通参数 | `name` | 标签名，如 `"p"`、`"img"` | 位置或关键字都可（除非你加 `/`） |
| `*content` | 可变位置参数 | 捕获多段内容，形成一个 tuple | `tag("p", "hello", "world")` |
| `class_` | 仅限关键字参数 | 专门处理 `class` 属性（避开关键字冲突） | 必须写 `class_="sidebar"` |
| `**attrs` | 可变关键字参数 | 捕获其余所有关键字参数，形成 dict | `tag("img", src="x.png", title="...")` |

### 1.1 `class_` 为什么要这么写

因为 `class` 是 Python 关键字，不能写成参数名：

- 不能写：`def tag(..., class=None):`（语法错误）
- 常见替代：`class_` / `klass` / `css_class`

然后函数内部把它转成真正的 HTML 属性名：

```python
attrs["class"] = class_
```

---

## 二、一个容易写错的点：生成器不能 “if attr_pairs”

有的人会写：

```python
attr_pairs = (f'{k}="{v}"' for k, v in attrs.items())
if attr_pairs:
    ...
```

这是个坑：`attr_pairs` 是生成器对象，**它本身永远为真**，即使里面没有元素。

正确做法是：

- 要么先构造成 list：`pairs = [...]`
- 要么判断 `attrs` 本身是否为空：`if attrs:`

配套脚本里用的是“先 list 再 join”的写法，逻辑最直观。

---

## 三、示例 7-10：调用方式为什么这么灵活

你会在 demo 里看到这些调用方式（都能工作）：

- `tag("h1", "hello")`：一个内容
- `tag("p", "hello", "world")`：多个内容（`*content`）
- `tag("p", "hello", class_="sidebar")`：仅限关键字参数
- `tag("img", src="x.png", title="Sunset")`：可变关键字参数
- `tag("img", **{"src": "x.png", "title": "Sunset"})`：`**` 解包

关键直觉：

> 你把参数形态设计好，调用者就能自然地用“位置/关键字/解包”的方式组合调用。

---

## 四、`/`：仅限位置参数（Python 3.8+）

语法：

```python
def f(a, b, /, c, d):
    ...
```

含义：

- `/` **之前**：只能用位置传参（positional-only）
- `/` **之后**：默认既可位置也可关键字（除非再加 `*`）

### 4.1 为什么要有它

两个常见原因：

1. **兼容内置函数风格**：很多内置函数历史上就不接受关键字实参；现在你也能在 Python 层写出同样的约束。
2. **参数名不想成为 API 契约**：有些参数名只是实现细节，未来可能改名；强制位置能避免用户依赖参数名。

### 4.2 最小例子（demo 里会跑）

```python
def divmod_like(a, b, /):
    return (a // b, a % b)

divmod_like(10, 3)      # 合法
# divmod_like(a=10, b=3)  # TypeError：/ 左侧禁止关键字实参
```

---

## 五、参数类型速查（把规则一次记清楚）

| 类型 | 语法 | 说明 |
|---|---|---|
| 仅限位置参数 | `def f(a, /):` | 只能 `f(1)`，不能 `f(a=1)` |
| 普通参数 | `def f(a):` | 可位置可关键字 |
| 可变位置参数 | `def f(*args):` | 捕获多余的位置参数为 tuple |
| 仅限关键字参数 | `def f(*, a):` / `def f(*args, a):` | 必须写 `a=...` |
| 可变关键字参数 | `def f(**kwargs):` | 捕获多余关键字参数为 dict |

---

## 六、运行

```bash
python part-2-functions-as-objects/chapter-07/07_tag_and_positional_only_demo.py
```

你会看到：

- **`demo_beginner_walkthrough`**：新手向逐行注释 + 打印说明  
- **`demo_args_vs_kwargs_plain`**：**大白话 §9·三** 最小 `f(*args, **kwargs)`  
- **`demo_parameter_kinds_minimal`** / **`demo_full_parameter_template`**：**§零·二·附** 五类参数 + 全家桶一例  
- `tag` 的多种调用输出  
- `class_` 如何映射到 HTML 的 `class`  
- `**attrs` 如何拼装属性字符串  
- `/` 的 “禁止关键字实参” 的 TypeError 证据

