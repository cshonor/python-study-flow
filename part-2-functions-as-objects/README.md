# Part 2：函数作为对象与装饰器

本部分聚焦 Python 函数式编程与高级语法（对应《流畅的 Python》**Part 2** 范围）。

**命名约定**：各章目录内笔记为 `NN-<中文标题>.md`（标题一般取自该篇首个一级标题）；可运行示例为 `NN_<描述>_demo.py`（或 `NN_<描述>.py`），篇号与笔记的 `NN` 对齐；同一篇若对应多个脚本，则共用同一 `NN_` 前缀（例如第 9 章的 `02_…`）。

---

## 目录（章节）

| 目录 | 对应 | 说明 |
|------|------|------|
| `chapter-07/` | 第 7 章「函数是一等对象」 | `01` **§零** 极简背诵；高阶函数、`lambda`、可调用对象；见 `chapter-07/README.md` |
| `chapter-08/` | 第 8 章「函数中的类型提示」 | Type Hints 开篇：PEP 484、渐进式类型、动态/静态边界；见 `chapter-08/README.md` |
| `chapter-09/` | 第 9 章「装饰器和闭包」 | 作用域（LEGB）、闭包、装饰器从基础到参数化；见 `chapter-09/README.md` |
| `chapter-10/` | 第 10 章「用一等函数实现设计模式」 | 用函数/闭包简化策略、命令等模式；见 `chapter-10/README.md` |

---

## 核心知识点
- **一等对象**：函数是对象，可作为参数、返回值、存储在数据结构中。
- **高阶函数**：`map()`、`filter()`、`sorted()`、`functools.reduce()`。
- **闭包 (Closure)**：自由变量的延迟绑定，保持函数状态。
- **装饰器 (Decorator)**：
  - 函数装饰器与类装饰器。
  - 叠放装饰器 (@wrap)。
  - 带参数的装饰器。
- **函数式工具**：`functools` 模块，`partial` 偏函数，`singledispatch` 泛函数。
